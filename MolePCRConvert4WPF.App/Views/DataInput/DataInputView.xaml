<UserControl x:Class="MolePCRConvert4WPF.App.Views.DataInput.DataInputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MolePCRConvert4WPF.App.Views.DataInput"
             xmlns:vm="clr-namespace:MolePCRConvert4WPF.App.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:DataInputViewModel}"
             Background="{DynamicResource MaterialDesignPaper}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="0"> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="PCR数据导入" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="16,16,16,8"/>

        <StackPanel Grid.Row="1" Margin="16,8,16,16">
            
            <TextBlock Text="文件路径:" Margin="0,0,0,5" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" 
                         Text="{Binding SelectedFilePath, Mode=OneWay}" 
                         IsReadOnly="True" 
                         VerticalAlignment="Center"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         mah:TextBoxHelper.UseFloatingWatermark="False"/> 
                <Button Grid.Column="1" Content="浏览..." 
                        Command="{Binding BrowseCommand}" 
                        Margin="8,0,0,0" 
                        VerticalAlignment="Center" 
                        Style="{StaticResource MaterialDesignFlatButton}"/>
            </Grid>

            <TextBlock Text="仪器类型:" Margin="0,0,0,5" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
            <ComboBox ItemsSource="{Binding InstrumentTypes}" 
                      SelectedItem="{Binding SelectedInstrumentType}" 
                      Margin="0,0,0,16" 
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

            <TextBlock Text="分析方法:" Margin="0,0,0,5" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
            <ComboBox ItemsSource="{Binding AvailableAnalysisMethods}" 
                      SelectedItem="{Binding SelectedAnalysisMethod}" 
                      Margin="0,0,0,16"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

        </StackPanel>

        <Border Grid.Row="2" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,1,0,0">
            <Grid Margin="16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="取消" 
                            Width="80" 
                            Margin="0,0,8,0" 
                            IsCancel="True" 
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"/> 
                    <Button Content="导入" 
                            Width="100" 
                            Command="{Binding LoadDataCommand}" 
                            IsEnabled="{Binding Path=IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                            Style="{StaticResource MaterialDesignRaisedButton}"/> 
                </StackPanel>
                
                <materialDesign:Card HorizontalAlignment="Left" Padding="4" VerticalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel Orientation="Horizontal">
                         <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" Width="18" Height="18" Margin="0,0,8,0"/>
                         <TextBlock Text="加载中..." VerticalAlignment="Center" Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </Border>
    </Grid>
</UserControl> 