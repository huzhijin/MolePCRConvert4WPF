<UserControl x:Class="MolePCRConvert4WPF.App.Views.Dialogs.PatientInfoDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MolePCRConvert4WPF.App.Views.Dialogs"
             xmlns:vm="clr-namespace:MolePCRConvert4WPF.App.ViewModels.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:PatientInfoDialogViewModel}"
             d:DesignWidth="300" MaxWidth="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="0,0,0,15" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="输入患者信息"/>

        <StackPanel Grid.Row="1">
            <TextBox Margin="0,0,0,10"
                     materialDesign:HintAssist.Hint="患者姓名"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding PatientName, UpdateSourceTrigger=PropertyChanged}" />
                     <!-- Add ValidationAssist if using validation -->
                     <!-- materialDesign:ValidationAssist.HasError="{Binding HasErrors}" -->

            <TextBox Margin="0,5,0,10"
                     materialDesign:HintAssist.Hint="病历号"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Text="{Binding MedicalRecordNumber, UpdateSourceTrigger=PropertyChanged}" />
                     <!-- Add ValidationAssist if using validation -->
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Margin="0,0,8,0"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="{Binding}" 
                    Content="确定"
                    IsDefault="True"
                    Style="{StaticResource MaterialDesignFlatButton}">
                 <!-- Pass the ViewModel back as the result when OK is clicked -->
            </Button>
            <Button Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    CommandParameter="False" 
                    Content="取消"
                    IsCancel="True"
                    Style="{StaticResource MaterialDesignFlatButton}">
                 <!-- Pass False back when Cancel is clicked -->
            </Button>
        </StackPanel>
    </Grid>
</UserControl> 