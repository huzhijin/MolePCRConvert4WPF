<UserControl x:Class="MolePCRConvert4WPF.App.Views.Analysis.PCRResultAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MolePCRConvert4WPF.App.Views.Analysis"
             xmlns:vm="clr-namespace:MolePCRConvert4WPF.App.ViewModels"
             xmlns:models="clr-namespace:MolePCRConvert4WPF.Core.Models"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:conv="clr-namespace:MolePCRConvert4WPF.App.Converters"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:PCRResultAnalysisViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
         <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
         <conv:InverseBooleanConverter x:Key="InverseBooleanConverter" />
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="PCR 结果分析" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,10"/>

        <!-- Action Buttons and Info -->
        <materialDesign:Card Grid.Row="1" Padding="10" Margin="0,0,0,10" UniformCornerRadius="8">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        ToolTip="重新加载数据并执行分析"
                        Command="{Binding RefreshCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" 
                        Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal">
                         <materialDesign:PackIcon Kind="Refresh" Margin="0,0,5,0" VerticalAlignment="Center"/>
                         <TextBlock Text="刷新分析"/>
                    </StackPanel>
                </Button>
                
                 <!-- Report Buttons -->
                 <Button Content="生成患者报告" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding GeneratePatientReportCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" 
                        Margin="0,0,8,0"/>
                <Button Content="生成整板报告" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding GeneratePlateReportCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" 
                        Margin="0,0,16,0"/>
                 
                 <!-- Add Export Button if needed -->
                 <!-- 
                 <Button Content="导出结果" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding ExportCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" 
                        Margin="0,0,16,0"/> 
                 -->

                <TextBlock Text="分析方法:" VerticalAlignment="Center" FontWeight="Medium" Margin="10,0,5,0" Opacity="0.7"/>
                <TextBlock Text="{Binding CurrentAnalysisMethodName, FallbackValue='加载中...'}" VerticalAlignment="Center" ToolTip="当前分析所使用的配置方法文件"/>

                 <!-- Loading Indicator -->
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" 
                             Value="0" IsIndeterminate="True" 
                             Width="20" Height="20" 
                             Margin="10,0,0,0" VerticalAlignment="Center"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Results Grid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding ResultEntries}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  materialDesign:DataGridAssist.CellPadding="13 8 13 8" 
                  materialDesign:DataGridAssist.ColumnHeaderPadding="13 8 13 8"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
            <DataGrid.Columns>
                <materialDesign:DataGridTextColumn Header="孔位" Binding="{Binding WellPosition}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <materialDesign:DataGridTextColumn Header="通道" Binding="{Binding Channel}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <materialDesign:DataGridTextColumn Header="样本名称" Binding="{Binding SampleName}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <materialDesign:DataGridTextColumn Header="种名" Binding="{Binding SpeciesName}" Width="1.5*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <materialDesign:DataGridTextColumn Header="Ct值" Binding="{Binding CtValue, StringFormat={}{0:F2}, FallbackValue='--'}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                <materialDesign:DataGridTextColumn Header="判定结果" Binding="{Binding JudgementResult}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}" FontWeight="Bold"/>
                <materialDesign:DataGridTextColumn Header="浓度" Binding="{Binding Concentration, StringFormat={}{0:F3}, FallbackValue='--'}" Width="Auto" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/>
                 <!-- Optional: Display Formulas Used -->
                 <!-- <materialDesign:DataGridTextColumn Header="判定公式" Binding="{Binding JudgeFormula}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/> -->
                 <!-- <materialDesign:DataGridTextColumn Header="浓度公式" Binding="{Binding ConcentrationFormula}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}"/> -->
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</UserControl> 