<UserControl x:Class="MolePCRConvert4WPF.App.Views.ReportTemplateDesignerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:MolePCRConvert4WPF.App.Controls"
             xmlns:reoGrid="clr-namespace:unvell.ReoGrid;assembly=unvell.ReoGrid"
             xmlns:vm="clr-namespace:MolePCRConvert4WPF.App.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:ReportTemplateDesignerViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <Style x:Key="VariableCategoryStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <Style x:Key="VariableItemStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="5,2"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <ToolBar DockPanel.Dock="Top" Height="40">
            <Button Command="{Binding NewTemplateCommand}" ToolTip="创建新模板 (Ctrl+N)" Padding="5,2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="新建" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Separator/>
            <Button Command="{Binding SaveCommand}" ToolTip="保存 (Ctrl+S)" Padding="5,2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="保存" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button Command="{Binding SaveAsCommand}" ToolTip="另存为 (Ctrl+Shift+S)" Padding="5,2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="另存为" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </ToolBar>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="报告模板设计器" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Margin="0,0,0,16" />

        <!-- 左侧变量面板 -->
        <Grid DockPanel.Dock="Left" Width="250" Background="#F0F0F0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Grid.Row="0" Text="可用变量" FontWeight="Bold" Margin="10,10,0,5" FontSize="14"/>
            
            <controls:TemplateVariablesPanel 
                Grid.Row="1" 
                VariablesByCategory="{Binding VariableCategoryGroups}" 
                InsertVariableCommand="{Binding InsertVariableCommand}" />
        </Grid>

        <!-- ReoGrid控件 -->
        <Border Grid.Row="2" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1">
            <Grid Background="White">
                <ContentControl>
                    <reoGrid:ReoGridControl x:Name="ReoGridControl" />
                </ContentControl>
            </Grid>
        </Border>

        <!-- 状态栏 -->
        <StatusBar DockPanel.Dock="Bottom" Height="25">
            <TextBlock Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="5,0"/>
        </StatusBar>
    </Grid>
</UserControl> 