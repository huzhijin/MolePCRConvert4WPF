<Window x:Class="MolePCRConvert4WPF.TemplateFixWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="报告模板修复工具" Height="450" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,15">
            <Run FontWeight="Bold" FontSize="16">报告模板修复工具</Run>
            <LineBreak/>
            <Run>本工具用于修复报告模板中的常见问题，帮助解决"startIndex cannot be larger than length of string"错误。</Run>
            <LineBreak/>
            <Run>修复工具将：</Run>
            <LineBreak/>
            <Run>  1. 将旧格式占位符 [xxx] 替换为新格式 ${xxx}</Run>
            <LineBreak/>
            <Run>  2. 在数据表格开始处添加 [[DataStart]] 标记</Run>
            <LineBreak/>
            <Run>修复前将自动创建备份文件，原文件名后加.backup后缀</Run>
        </TextBlock>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnFixAll" Click="BtnFixAll_Click" Padding="10,5" Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="修复所有模板" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <Button x:Name="btnFixSelected" Click="BtnFixSelected_Click" Padding="10,5">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="修复选定模板" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </StackPanel>
        
        <ListView Grid.Row="2" x:Name="lvResults" Margin="0,0,0,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="模板文件" Width="200" DisplayMemberBinding="{Binding FileName}"/>
                    <GridViewColumn Header="状态" Width="70">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" Foreground="{Binding StatusColor}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="详细信息" Width="280" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnClose" Click="BtnClose_Click" Content="关闭" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window> 